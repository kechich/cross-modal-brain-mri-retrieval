# Copy to .env and fill in. .env is gitignored — never commit real secrets.

# Kaggle API token (for downloading competition data / submitting)
KAGGLE_API_TOKEN=

# Remote JupyterLab box used for GPU training (see docs/workflow.md)
JUPYTER_HOST=
JUPYTER_TOKEN=
